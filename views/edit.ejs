<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit message </title>
</head>
<body>
    <h3>Edit your chat whose id is : <%=chat._id%></h3><br>
    <p>This chat was sent from <b> <%=chat.from%> </b> to <b><%=chat.to%> </b></p><br>

    <form action="/chats/<%=chat.id%>?_method=PUT" method="POST">
        <textarea name="msg" cols="50" rows="10">
            <%=chat.msg%>
        </textarea><br>
        <button>Edit</button>
    </form>
</body>
</html>